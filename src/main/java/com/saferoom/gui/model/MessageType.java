package com.saferoom.gui.model;

/**
 * Categorises chat messages so the UI can render the correct bubble.
 */
public enum MessageType {
    TEXT,
    IMAGE,
    VIDEO,
    DOCUMENT,
    FILE,
    FILE,
    FILE_PLACEHOLDER,
    TERMINAL_SESSION,
    LIVE_SESSION
}
